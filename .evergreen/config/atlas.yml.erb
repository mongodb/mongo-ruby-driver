buildvariants:
  - matrix_name: "all"
    matrix_spec: { ruby: '*' }
    #matrix_spec: { }
    display_name: "Atlas tests ${ruby}"
    run_on:
      - ubuntu1804-small
    tasks:
       - name: test-atlas

  - matrix_name: "serverless"
    matrix_spec: { ruby: 'ruby-3.0' }
    #matrix_spec: { }
    display_name: "Atlas serverless tests ${ruby}"
    run_on:
      - ubuntu1804-small
    tasks:
       - name: test-serverless
