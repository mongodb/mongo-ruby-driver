# This file was auto-generated by .evergreen/make-github-actions
# at 2021-09-08T09:43:46Z
---
name: Run Mongoid Tests
'on':
- push
- pull_request
jobs:
  build:
    name: "${{matrix.ruby}} mongodb-${{matrix.mongodb}} ${{matrix.topology}}"
    env:
      CI: true
      TESTOPTS: "-v"
    runs-on: ubuntu-latest
    continue-on-error: "${{matrix.experimental}}"
    strategy:
      fail-fast: false
      matrix:
        os: ubuntu-latest
        ruby: [2.5, 2.6, 2.7, 3.0]
        mongodb: [3.6, 4.4, 5.0]
        topology: [replica_set, sharded_cluster]
        include:
          - os: macos
            ruby: 3.0
            mongodb: 5.0
            topology: server
          - os: ubuntu-latest
            ruby: 3.0
            mongodb: 5.0
            topology: server
          - os: ubuntu-18.04
            ruby: 2.5
            mongodb: 3.6
            topology: replica_set
          - os: ubuntu-16.04
            ruby: 2.4
            mongodb: 3.6
            topology: replica_set
    steps:
    - name: repo checkout
      uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: start mongodb
      uses: mongodb-labs/drivers-evergreen-tools@master
      with:
        version: "${{matrix.mongodb}}"
        topology: "${{matrix.topology}}"
    - name: load ruby
      uses: ruby/setup-ruby@v1
      env:
        BUNDLE_GEMFILE: "${{matrix.gemfile}}"
      with:
        ruby-version: "${{matrix.ruby}}"
        bundler: 2
    - name: bundle
      run: bundle install --jobs 4 --retry 3
      env:
        BUNDLE_GEMFILE: "${{matrix.gemfile}}"
    - name: test
      timeout-minutes: 60
      continue-on-error: "${{matrix.experimental}}"
      run: bundle exec rake spec:ci
      env:
        BUNDLE_GEMFILE: "${{matrix.gemfile}}"
        MONGODB_URI: ${{ steps.setup-mongodb.outputs.cluster-uri }}
